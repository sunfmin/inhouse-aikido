# v1 opens a Remediation as a pull request

A Finding can carry a Remediation: HQ writes a PR that should Fixed it (bump a pin, delete a secret, change IaC). We rejected annotation-only and a later-only seam — v1 is a bot that commits. When HQ opens that PR, and for which Findings, is still open.
