# The Operator's surface is the HQ CLI, not a web UI

Enrollment, the Dismissed list, and Reopen happen on a CLI that talks to the HQ. Developers still only see PR annotations. We rejected a thin HQ dashboard and GitHub-as-the-Operator-console. v1 has no web UI.
