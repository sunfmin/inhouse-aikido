# A Remediation is the edit, not the Finding

One pin-bump is one PR, linked to every Finding that bump would Fixed. Two packages are two Remediations. We rejected one-PR-per-Finding (conflicting edits to the same line) and one-weekly-PR-per-Target (unreviewable).
