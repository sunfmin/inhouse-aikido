# A Developer can Dismiss, and that passes the Gate

Anyone who can write the Target can Dismiss a Finding, including a new secret. That is the Gate's escape hatch. The Operator reviews the Dismissed list and Reopens; v1 has no approval workflow. A secret is a Finding, not a second kind of object. We rejected Operator-only dismiss (Developer-first dies) and a secrets/severity split.
