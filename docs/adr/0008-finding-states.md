# A Finding is Open, Fixed, or Dismissed

Open while the latest Scan still sees it. Fixed when a later Scan does not. Dismissed is a human decision on that Finding and survives new Revisions — including if the Observation disappears and later returns. We rejected having no dismiss (every PR nags forever) and we rejected a fourth Accepted-risk state in v1.
