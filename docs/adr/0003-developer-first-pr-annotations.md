# Developers see findings on the PR

A finding's primary surface is a pull-request annotation. The Operator writes rules and handles exceptions; they are not an inbox. We rejected a security-queue-first product and we rejected building two equal UIs in v1.
