# A Finding is one problem on one kind of Target

HQ merges Observations that are the same problem on the same kind of Target, so the Developer dismisses once. The same CVE in a lockfile and in a container image are two Findings — the fixes are different jobs. We rejected one-hit-one-Finding (engines never meet) and we rejected collapsing every mention of a CVE into a single Finding across Target kinds.
