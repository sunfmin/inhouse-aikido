# The location key includes the manifest path

Same CVE in `web/package-lock.json` and `worker/package-lock.json` are two Findings. Dismiss and Remediation are per edit. We rejected package-name-only (one Dismiss would silence the other lockfile; one bump would claim both Fixed).
